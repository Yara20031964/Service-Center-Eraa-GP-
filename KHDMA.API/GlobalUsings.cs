// Every controller carries a [Tags(ApiTags.X)] attribute to place it in the right
// Swagger section, so the tag constants are pulled in project-wide rather than
// repeating the same using in ~27 files.
global using KHDMA.API.Swagger;
